1. redux-thunk - это middleware-усилитель, который получает action,
проверяет, если action - функция, передает в нее метод dispatch и state и вызывает,
а если объект, то пропускает.

Внутри такой функции диспатчится синхронный action, а затем асинхронный.
